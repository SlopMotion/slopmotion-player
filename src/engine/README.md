# Corresponding source

TypeScript, GLSL, and data that `hydra-live-hexa` compiles into `dist/index.js`.
Regenerated by `npm run player:build` in hydra-live-hexa.

This snapshot has 230 files.

`data/videoLibrary.json` is empty and `data/playsetPacks.json` is folder names only —
the public bundle does not ship the Studio clip catalog.

hydra-synth (AGPL-3.0) is bundled from npm; its source is https://github.com/ojack/hydra-synth
